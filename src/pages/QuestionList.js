  
import React from 'react'
import Question from './Question'

export default function QuestionList({ todos, toggleTodo }) {
  return (
    // todos.map(todo => {
    //   return <Todo key={todo.id} toggleTodo={toggleTodo} todo={todo} />
    // })
    <div>
      Hello world 
    </div>
  )
}